# Build settings
[build]
  command = "npm run build"
  publish = "dist"

# Redirects for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables placeholder
# Configure estas variables en Netlify Dashboard:
# Site settings → Environment variables
# VITE_API_URL = https://back-proyecto-ej.onrender.com

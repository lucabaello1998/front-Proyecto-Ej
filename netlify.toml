# Build settings
[build]
  command = "npm run build"
  publish = "dist"

# Redirects for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables placeholder
# ⚠️ IMPORTANTE: Configure esta variable en Netlify Dashboard:
# Site settings → Environment variables → Add a variable
# Key: VITE_API_URL
# Value: https://back-proyecto-ej.onrender.com
# Luego haga un nuevo deploy para que tome efecto
